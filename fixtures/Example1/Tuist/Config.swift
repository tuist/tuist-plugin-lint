import ProjectDescription

let config = Config(
    plugins: [
        // .git(url: "https://github.com/tuist/tuist-plugin-lint", tag: "0.2.0") // Uncomment to test released plugin and specify the desired version
    ]
)
